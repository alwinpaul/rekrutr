const get_domain = "http://localhost:4000";

const apiUrls = {
  GET_CONFIG: `${get_domain}/api/config`,
  ADD_CANDIDATE: `${get_domain}/api/candidates`,
  GET_CANDIDATES: `${get_domain}/api/candidates`,
};

export default apiUrls;
